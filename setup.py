from setuptools import setup, find_packages

setup(
    name='brain',
    version='0.1',
    packages=find_packages(),
    description='main processors for attack surface monitoring',
    author='Samuel Paredes',
    author_email='sam@bugnode.io',
)
